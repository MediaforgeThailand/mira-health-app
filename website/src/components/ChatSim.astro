<div class="phone-frame" data-chat-sim aria-label="ตัวอย่างบทสนทนา Mira">
  <div class="phone-top">
    <span class="camera" aria-hidden="true"></span>
    <div>
      <strong>Mira Chat</strong>
      <span><i class="online-dot" aria-hidden="true"></i>กำลังขายอยู่ตอนนี้</span>
    </div>
  </div>
  <div class="chat-stream" data-chat-stream>
    <noscript>
      <div class="chat-noscript">Mira แนะนำสินค้า รับออเดอร์ ชำระเงิน และคำนวณส่วนแบ่งผู้แนะนำในแชทเดียว</div>
    </noscript>
  </div>
</div>

<style>
  .phone-frame {
    position: relative;
    width: min(100%, 390px);
    min-height: 650px;
    margin-inline: auto;
    border: 1px solid rgba(214, 230, 255, 0.22);
    border-radius: 34px;
    background:
      linear-gradient(180deg, rgba(246, 249, 253, 0.97), rgba(236, 242, 252, 0.95)),
      var(--surface);
    box-shadow:
      0 36px 90px -42px rgba(46, 107, 246, 0.85),
      inset 0 0 0 8px rgba(5, 18, 38, 0.88);
    color: var(--ink);
    overflow: hidden;
  }

  .phone-frame::before {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    content: "";
    background:
      radial-gradient(circle at 18% 10%, rgba(46, 107, 246, 0.16), transparent 34%),
      radial-gradient(circle at 90% 22%, rgba(127, 177, 254, 0.14), transparent 28%);
  }

  .phone-top {
    position: relative;
    z-index: 1;
    display: flex;
    gap: 0.75rem;
    align-items: center;
    min-height: 78px;
    margin: 8px;
    border-bottom: 1px solid var(--line);
    border-radius: 26px 26px 0 0;
    padding: 1.2rem 1.25rem 0.85rem;
  }

  .camera {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background:
      radial-gradient(circle at 50% 50%, var(--sky) 0 20%, transparent 21%),
      var(--night);
    box-shadow: 0 0 0 4px rgba(46, 107, 246, 0.14);
  }

  strong,
  span {
    display: block;
  }

  strong {
    color: var(--ink);
    font-family: "Space Grotesk", system-ui, sans-serif;
    font-size: 0.98rem;
    line-height: 1.2;
  }

  .phone-top span:not(.camera) {
    display: inline-flex;
    gap: 0.32rem;
    align-items: center;
    color: var(--ink-soft);
    font-size: 0.73rem;
    line-height: 1.35;
  }

  .online-dot {
    width: 7px;
    height: 7px;
    flex: none;
    border-radius: 50%;
    background: var(--mint);
    box-shadow: 0 0 0 3px rgba(84, 207, 165, 0.22);
  }

  .chat-stream {
    position: relative;
    z-index: 1;
    display: flex;
    height: 560px;
    flex-direction: column;
    gap: 0.72rem;
    overflow: hidden;
    padding: 1rem 1.1rem 1.25rem;
  }

  .chat-noscript {
    margin-top: auto;
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    background: var(--surface);
    padding: 1rem;
  }

  :global(.chat-item) {
    width: fit-content;
    max-width: 86%;
    transform: translateY(10px);
    animation: chatIn 360ms ease forwards;
  }

  :global(.chat-item.user) {
    align-self: flex-end;
    border-radius: 18px 18px 6px 18px;
    background: var(--grad-brand);
    color: white;
    padding: 0.72rem 0.9rem;
  }

  :global(.chat-item.mira) {
    align-self: flex-start;
    border: 1px solid var(--line);
    border-radius: 18px 18px 18px 6px;
    background: var(--surface);
    box-shadow: 0 10px 22px rgba(34, 68, 134, 0.08);
    color: var(--ink);
    padding: 0.72rem 0.9rem;
  }

  :global(.chat-chip) {
    align-self: center;
    max-width: 100%;
    border: 1px solid var(--line);
    border-radius: var(--radius-pill);
    background: rgba(255, 255, 255, 0.76);
    color: var(--ink-soft);
    font-size: 0.74rem;
    padding: 0.42rem 0.72rem;
    text-align: center;
  }

  :global(.chat-chip.status) {
    border-color: rgba(84, 207, 165, 0.54);
    background: rgba(84, 207, 165, 0.16);
    color: var(--mint-deep);
    font-weight: 600;
  }

  :global(.typing) {
    display: inline-grid;
    grid-template-columns: repeat(3, 6px);
    gap: 4px;
    align-items: center;
  }

  :global(.typing span) {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--muted);
    animation: typingPulse 900ms infinite ease-in-out;
  }

  :global(.typing span:nth-child(2)) {
    animation-delay: 120ms;
  }

  :global(.typing span:nth-child(3)) {
    animation-delay: 240ms;
  }

  :global(.product-stack) {
    display: grid;
    width: 100%;
    max-width: 100%;
    grid-template-columns: 1fr 1fr;
    gap: 0.55rem;
  }

  :global(.product-card),
  :global(.order-card),
  :global(.toast-card) {
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    background: var(--surface);
    box-shadow: 0 10px 22px rgba(34, 68, 134, 0.08);
    padding: 0.75rem;
  }

  :global(.product-card strong),
  :global(.order-card strong) {
    color: var(--ink);
    font-family: "Anuphan", sans-serif;
    font-size: 0.82rem;
    line-height: 1.35;
  }

  :global(.product-card .price) {
    color: var(--brand-deep);
    font-family: "Space Grotesk", sans-serif;
    font-weight: 600;
  }

  :global(.sample-badge) {
    display: inline-flex;
    width: fit-content;
    margin-top: 0.45rem;
    border-radius: var(--radius-pill);
    background: var(--brand-soft);
    color: var(--brand-deep);
    font-size: 0.68rem;
    font-weight: 600;
    padding: 0.12rem 0.42rem;
  }

  :global(.order-card) {
    display: grid;
    width: 100%;
    grid-template-columns: 1fr 76px;
    gap: 0.75rem;
    align-items: center;
  }

  :global(.order-card p) {
    margin: 0.25rem 0 0;
    color: var(--ink-soft);
    font-size: 0.72rem;
    line-height: 1.45;
  }

  :global(.qr-tile) {
    display: grid;
    aspect-ratio: 1;
    place-items: center;
    border-radius: 12px;
    background:
      linear-gradient(90deg, var(--ink) 12px, transparent 0) 0 0 / 24px 24px,
      linear-gradient(var(--ink) 12px, transparent 0) 0 0 / 24px 24px,
      var(--canvas);
    color: var(--surface);
    font-family: "Space Grotesk", sans-serif;
    font-size: 0.72rem;
    font-weight: 600;
    overflow: hidden;
  }

  :global(.toast-card) {
    width: 100%;
    border-color: rgba(84, 207, 165, 0.5);
    background: linear-gradient(110deg, rgba(46, 107, 246, 0.12), rgba(84, 207, 165, 0.2));
    color: var(--mint-deep);
    font-weight: 600;
  }

  :global(.is-resetting) {
    animation: chatReset 400ms ease forwards;
  }

  @keyframes chatIn {
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes typingPulse {
    0%,
    80%,
    100% {
      transform: translateY(0);
      opacity: 0.45;
    }

    40% {
      transform: translateY(-3px);
      opacity: 1;
    }
  }

  @keyframes chatReset {
    to {
      opacity: 0;
      transform: translateY(-8px);
    }
  }

  @media (max-width: 520px) {
    .phone-frame {
      min-height: 590px;
      border-radius: 28px;
    }

    .chat-stream {
      height: 504px;
      padding-inline: 0.9rem;
    }

    :global(.product-stack) {
      grid-template-columns: 1fr;
    }
  }
</style>
